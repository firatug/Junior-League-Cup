@{
    var formType = ViewData["FormType"] as string ?? "form";
    var (title, message) = FormThankYouLocalization.GetContent(formType);
    ViewData["Title"] = title;
    ViewData["Description"] = message;
}

<section class="page-hero">
    <div class="container">
        <h1>@title.ToUpperInvariant()</h1>
        <p class="lead">@message</p>
        <p class="breadcrumb"><a asp-action="Index">Ana Sayfa</a> / Teşekkürler</p>
    </div>
</section>

<section class="content-section">
    <div class="container" style="max-width:640px;text-align:center;">
        <div class="admin-card" style="padding:2.5rem;">
            <div style="font-size:3rem;margin-bottom:1rem;" aria-hidden="true">✓</div>
            <h2 style="margin-bottom:1rem;">@title</h2>
            <p style="color:var(--text-muted);line-height:1.7;margin-bottom:2rem;">@message</p>
            <div style="display:flex;flex-wrap:wrap;gap:0.75rem;justify-content:center;">
                <a asp-action="Index" class="btn btn-primary">Ana Sayfaya Dön</a>
                @if (formType == JuniorLeagueCup.WebSite.Models.FormThankYouTypes.TakimBasvurusu)
                {
                    <a asp-action="BolgeselSecmeler" class="btn btn-outline-dark">Bölgesel Seçmeler</a>
                }
                else if (formType == JuniorLeagueCup.WebSite.Models.FormThankYouTypes.Sponsorluk)
                {
                    <a asp-action="Sponsorluk" class="btn btn-outline-dark">Sponsorluk</a>
                }
                else
                {
                    <a asp-action="Medya" class="btn btn-outline-dark">Haberler</a>
                }
            </div>
        </div>
    </div>
</section>
